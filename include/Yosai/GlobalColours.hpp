//
// Created by Roman Fiskov (roman.fiskov@gmail.com) [Mr.Fiskerton] on 12.07.18.
//

#ifndef INCLUDED_GLOBALCOLOURS_HPP
#define INCLUDED_GLOBALCOLOURS_HPP

#include <SFML/Graphics/Color.hpp>

namespace colour {
    const sf::Color WHITE = sf::Color(252, 246, 246);
    const sf::Color AZURE = sf::Color(95, 184, 200);
    const sf::Color RED   = sf::Color(189, 97, 79);
    const sf::Color GREY  = sf::Color(233, 230, 229);
}
#endif //INCLUDED_GLOBALCOLOURS_HPP
